#include "main.h"
#include <stdio.h>
/*
 *  * Add: adds tw integers and prints results
 *   * @i: print int i
 *    * @k: print int k
 *     * Return: Always 0.
 */
int add(int i, int k)
{
return (i + k);
}
